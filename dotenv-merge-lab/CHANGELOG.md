# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **dotenv-merge-lab** MCP server.
- Tools: `dotenv_parse`, `dotenv_merge`, `dotenv_diff_keys`, `dotenv_redact`.
- Skills: `dotenv-parse-merge`, `dotenv-diff-redact`.
- String-level .env analysis only — no secret-file reads, filesystem walks, or network I/O.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
