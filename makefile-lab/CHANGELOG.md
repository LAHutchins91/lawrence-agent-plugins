# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **makefile-lab** MCP server.
- Tools: `make_targets_list`, `make_phony_list`, `make_var_lookup`, `make_lint_lite`.
- Pure Makefile text heuristics; no `make` binary, no network, no shell execution.
