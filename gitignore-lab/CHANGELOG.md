# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **gitignore-lab** MCP server.
- Tools: `gi_match`, `gi_parse`, `gi_explain`, `gi_merge`.
- Skills: `gi-match-explain`, `gi-parse-merge`.
- Hand-rolled gitignore-style matcher (`*`, `**`, `?`, trailing `/`, `!`); no FS walks.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
