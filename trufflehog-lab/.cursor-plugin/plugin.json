{
  "name": "trufflehog-lab",
  "displayName": "TruffleHog Lab",
  "version": "1.0.0",
  "description": "Local zero-auth MCP for TruffleHog config/detector heuristics — detector names, exclude-paths / exclude-detectors / filter-entropy / include-paths filter counts, --json / --only-verified / --results / git / github / filesystem / s3 / --concurrency config hints, and lite lint — pasted source only, never runs trufflehog CLI, never returns secret values, no network.",
  "author": {
    "name": "Lawrence Hutchins"
  },
  "license": "MIT",
  "keywords": ["mcp", "trufflehog", "secrets", "config", "local", "zero-auth"],
  "category": "developer-tools",
  "tags": ["mcp", "local", "trufflehog", "secrets", "zero-auth", "FREE"],
  "logo": "assets/logo.svg",
  "mcpServers": "./mcp.json"
}
