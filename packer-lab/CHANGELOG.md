# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **packer-lab** MCP server.
- Tools: `packer_builds_list`, `packer_sources_hint`, `packer_provisioners_hint`, `packer_lint_lite`.
- Skills: `packer-builds-sources`, `packer-provisioners-lint`.
- Pure Packer HCL/JSON string heuristics — no packer CLI, build/deploy, or network.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
