---
description: Fail CI when secrets appear in code comments (sk_live, AKIA, JWT, conn strings).
globs:
  - "**/*"
alwaysApply: false
---
# SecretsInCommentsBan

Fail CI when secrets appear in code comments (sk_live, AKIA, JWT, conn strings).

Report findings with paths and remediation. Do not invent findings.
Marketplace plugin is free forever; Pro CI at https://plugins.lawrence.dev/pro
