# Changelog

All notable changes to **license-spdx-lab** are documented here.

## 1.0.0 — 2026-09-13

- Add curated `spdx_lookup` and fuzzy `spdx_normalize` MCP tools.
- Add heuristic `license_compat_hint` with an explicit NOT-legal-advice disclaimer.
- Add `license_header_suggest` short headers (SPDX pointer for copyleft; no GPL text dumps).
- Add skills, smoke tests, and Cursor plugin packaging. Local only; no network.
