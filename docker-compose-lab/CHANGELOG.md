# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **docker-compose-lab** MCP server.
- Tools: `compose_list_services`, `compose_ports_map`, `compose_env_keys`, `compose_lint_lite`.
- Skills: `compose-services-ports`, `compose-env-lint`.
- YAML string analysis only — no docker daemon, no filesystem compose file reads beyond provided text.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
