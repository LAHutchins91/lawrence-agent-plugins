# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **cypress-config-lab** MCP server.
- Tools: `cy_e2e_summary`, `cy_component_summary`, `cy_env_keys`, `cy_lint_lite`.
- Pure Cypress config text heuristics (JSON/JSONC + defineConfig / JS/TS best-effort); no cypress binary, no network.
