# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **ssh-config-lab** MCP server.
- Tools: `ssh_config_parse`, `ssh_host_resolve`, `ssh_config_lint`, `ssh_config_redact`.
- Skills: `ssh-parse-resolve`, `ssh-lint-redact`.
- OpenSSH config text analysis only — no SSH execution, network, DNS, filesystem, or private-key reads.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
