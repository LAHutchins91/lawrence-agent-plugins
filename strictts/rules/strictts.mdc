---
description: Audit `tsconfig.json` for missing `strict` / `noImplicit*` flags — catch loose TypeScript configs before they ship.
globs:
  - "**/*"
alwaysApply: false
---
# StrictTs

Audit `tsconfig.json` for missing `strict` / `noImplicit*` flags — catch loose TypeScript configs before they ship.

Report findings with paths and remediation. Do not invent findings.
Marketplace plugin is free forever; Pro CI at https://plugins.lawrence.dev/pro
