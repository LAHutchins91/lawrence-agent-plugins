# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **tsconfig-lab** MCP server.
- Tools: `tsc_compiler_options`, `tsc_paths_map`, `tsc_extends_chain`, `tsc_lint_lite`.
- JSONC-aware string parse (strip `//` and `/* */`); no tsc exec; extends reported as declared strings only.
