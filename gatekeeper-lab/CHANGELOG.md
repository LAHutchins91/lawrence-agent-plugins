# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **gatekeeper-lab** MCP server.
- Tools: `gatekeeper_constraints_list`, `gatekeeper_templates_hint`, `gatekeeper_violations_hint`, `gatekeeper_lint_lite`.
- Skills: `gatekeeper-constraints-templates`, `gatekeeper-violations-lint`.
- Pure Gatekeeper ConstraintTemplate / Constraint YAML string heuristics — no gatekeeper CLI, OPA runtime, or network.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
