# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **eslint-config-lab** MCP server.
- Tools: `eslint_extends_list`, `eslint_rules_summary`, `eslint_env_parser`, `eslint_lint_lite`.
- Prefer robust JSONC; YAML via `yaml` package; best-effort JS string heuristics for `.eslintrc.js` / `eslint.config.js`. No eslint binary; no network.
