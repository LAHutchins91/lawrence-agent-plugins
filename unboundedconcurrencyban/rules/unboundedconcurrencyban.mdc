---
description: Fail CI on Promise.all(list.map(async)) unbounded fan-out.
globs:
  - "**/*"
alwaysApply: false
---
# UnboundedConcurrencyBan

Fail CI on Promise.all(list.map(async)) unbounded fan-out.

Report findings with paths and remediation. Do not invent findings.
Marketplace plugin is free forever; Pro CI at https://plugins.lawrence.dev/pro
