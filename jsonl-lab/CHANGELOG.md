# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **jsonl-lab** MCP server.
- Tools: `jsonl_validate`, `jsonl_count`, `jsonl_slice`, `jsonl_to_array`.
- Skills: `jsonl-validate-count`, `jsonl-slice-array`.
- Caps: slice default limit 50 (max 500); to_array default 200_000 bytes / 1000 lines.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
