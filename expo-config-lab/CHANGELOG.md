# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **expo-config-lab** MCP server.
- Tools: `expo_slug_name`, `expo_plugins_list`, `expo_scheme_list`, `expo_lint_lite`.
- Pure app.json / app.config text heuristics; no expo binary, no network.
