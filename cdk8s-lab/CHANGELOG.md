# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **cdk8s-lab** MCP server.
- Tools: `cdk8s_charts_list`, `cdk8s_imports_hint`, `cdk8s_resources_hint`, `cdk8s_lint_lite`.
- Skills: `cdk8s-charts-resources`, `cdk8s-imports-lint`.
- Pure cdk8s app / cdk8s.yaml / Chart.yaml-ish string/YAML/regex heuristics — no cdk8s CLI, cluster, or network.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
