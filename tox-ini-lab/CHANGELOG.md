# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **tox-ini-lab** MCP server.
- Tools: `tox_envlist`, `tox_commands_map`, `tox_deps_list`, `tox_lint_lite`.
- Pure tox.ini text heuristics; no tox binary, no network, no venv.
