# Changelog

## 1.0.0 — 2026-09-13

- Initial release of **ansible-lab** MCP server.
- Tools: `ansible_plays_list`, `ansible_roles_hint`, `ansible_vars_hint`, `ansible_lint_lite`.
- Skills: `ansible-plays-roles`, `ansible-vars-lint`.
- Pure Ansible playbook/role YAML string heuristics — no ansible CLI, SSH, or network.
- Marketplace packaging under `plugin-dist/` (MIT, developer-tools, FREE).
